pub const DEFAULT_CFG = .{
    .CHIP8_MEMORY_SIZE = 4096,
    .CHIP8_WIDTH = 64,
    .CHIP8_HEIGHT = 32,
    .CHIP8_MULTIPLIER = 20,
    .CHIP8_GPR = 16,
    .CHIP8_STACK = 16,
    .CHIP8_KEYS = 16,
    .CHIP8_CHAR_SET_SIZE = 0x50,
    .CHIP8_PROGRAM_LOAD_ADDR = 0x200,
};
